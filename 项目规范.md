# 项目规范 — 海水养殖水质数据分析系统

> C语言控制台应用 | 标准：C11 | 编码：UTF-8

---

## 一、构建与运行

### 编译命令

```bash
# 完整构建
gcc -std=c11 -Wall -Iinclude src/*.c -o seawater_analysis -lm

# 或使用 Makefile
make          # 构建
make clean    # 清理
```

### 运行

```bash
./seawater_analysis   # Linux/Mac
seawater_analysis.exe # Windows
```

### 编译器要求

| 项目 | 说明 |
|------|------|
| 语言标准 | C11 (`-std=c11`) |
| 编译选项 | `-Wall`（全警告） |
| 头文件路径 | `-Iinclude` |
| 链接库 | `-lm`（数学库） |
| 编译产物 | 不提交（已在 `.gitignore` 中） |

---

## 二、项目结构

```
project1/
├── CLAUDE.md          # 本规范文件（AI 助手上下文）
├── Makefile            # 构建配置
├── include/            # 头文件（.h）
│   ├── common.h        #   核心数据结构 + 全局常量
│   ├── data_io.h       #   模块一：数据读写
│   ├── preprocess.h    #   模块二：数据预处理
│   ├── analysis.h      #   模块三：统计分析
│   ├── model.h         #   模块四：预测模型
│   ├── auth.h          #   模块五：用户登录 & 权限
│   ├── backup.h        #   模块一：备份恢复
│   ├── ui.h            #   模块五：菜单界面
│   └── utils.h         #   工具函数
├── src/                # 源文件（.c）
│   ├── main.c          #   程序入口
│   ├── data_io.c       #   模块一实现
│   ├── preprocess.c    #   模块二实现
│   ├── analysis.c      #   模块三实现
│   ├── model.c         #   模块四实现
│   ├── auth.c          #   用户登录实现
│   ├── backup.c        #   备份恢复实现
│   ├── ui.c            #   菜单界面实现
│   └── utils.c         #   工具函数实现
├── data/
│   ├── raw/            #   原始 CSV 数据文件
│   └── processed/      #   预处理后的输出文件
├── backup/             #   数据备份文件
├── reports/            #   报告输出（概览、预警、分析报告）
├── docs/               #   设计论述文档
└── 修改声明/           #   修改内容声明
```

---

## 三、编码规范

### 3.1 命名规范

| 类别 | 规范 | 示例 |
|------|------|------|
| 文件名 | 小写 + 下划线 | `data_io.c`, `preprocess.h` |
| 结构体名 | 大驼峰 (PascalCase) | `WaterRecord`, `WaterDataset` |
| 枚举类型 | 大驼峰 | `UserRole`, `ParamType` |
| 枚举值 | 全大写 + 下划线 | `ROLE_ADMIN`, `PARAM_TEMP` |
| 函数名 | 小写 + 下划线 | `load_csv_data()`, `detect_and_handle_outliers()` |
| 全局常量宏 | 全大写 + 下划线 | `INITIAL_CAPACITY`, `PAGE_SIZE` |
| 局部变量/参数 | 小写 + 下划线 | `window_size`, `out_params` |
| 静态全局变量 | `g_` 前缀 + 小写 | `g_stats`, `g_sort_param_idx` |
| 静态函数 | 小写 + 下划线 | `is_missing_value()`, `compute_stddev()` |

### 3.2 头文件保护

每个头文件必须使用条件编译防止重复包含：

```c
#ifndef MODULE_NAME_H
#define MODULE_NAME_H

// ... 内容 ...

#endif // MODULE_NAME_H
```

### 3.3 注释风格

- 使用 `/* ── 注释内容 ── */` 风格的分隔注释标记模块段落
- 每个公开函数前应有 `/* ══ ... ══ */` 风格的文档块，说明功能、参数、返回值
- 关键算法步骤用 `/* ── 步骤N：描述 ── */` 标记
- 中文注释，简洁明了

### 3.4 函数设计原则

- **单一职责**：每个函数只做一件事
- **辅助函数用 `static`**：不暴露给外部的内部函数声明为 `static`
- **错误处理**：涉及内存分配的操作，失败时必须释放已分配资源并返回错误码/NULL
- **const 正确性**：只读参数用 `const` 修饰

### 3.5 内存管理

- 动态分配的 `malloc`/`calloc` 必须有对应的 `free`
- 异常退出路径也必须释放资源（避免泄漏）
- 使用 `calloc` 优于 `malloc`（自动清零，安全）

---

## 四、核心数据结构

```c
// 单条水质记录
typedef struct {
    char timestamp[32];    // 时间戳 "2025-01-01 12:00"
    double temp;           // 水温 ℃
    double salinity;       // 盐度 PSU
    double pH;             // pH值
    double DO;             // 溶解氧 mg/L
    double precipitation;  // 降水量 m
    double air_temp;       // 气温 ℃
    bool valid;            // 数据有效性标记（含缺失值则 false）
} WaterRecord;

// 数据集（动态数组）
typedef struct {
    WaterRecord *records;  // 动态数组指针
    size_t total_count;    // 总记录数
    size_t valid_count;    // 有效记录数
    size_t capacity;       // 当前分配容量
} WaterDataset;
```

### 参数合理范围（定义在 `common.h`）

| 参数 | 最小值 | 最大值 | 单位 |
|------|--------|--------|------|
| 水温 (temp) | -5 | 40 | ℃ |
| 盐度 (salinity) | 0 | 45 | PSU |
| pH | 6.5 | 9.0 | — |
| 溶解氧 (DO) | 0 | 15 | mg/L |
| 降水量 (precipitation) | 0 | 500 | m |
| 气温 (air_temp) | -10 | 50 | ℃ |

---

## 五、模块职责

### 模块一：数据基础操作（`data_io.c` + `backup.c`）
- CSV 文件读取（动态扩容、缺失值识别）
- CSV/二进制数据存储与性能对比
- 数据查询（分页、筛选、排序）
- 数据修改与删除（含自动备份）
- 数据备份与恢复

### 模块二：数据预处理（`preprocess.c`）
- 异常值检测与处理（两遍扫描法）
- 缺失值填充（均值逼近法）
- 移动平均滤波（多窗口对比）
- 预处理统计信息收集与输出

### 模块三：统计分析（`analysis.c`）⚠️ 待实现
- 基本统计量（均值、最值、标准差）
- 分段统计（凌晨缺氧预警、盐度突变预警）
- 皮尔逊相关系数矩阵

### 模块四：预测模型（`model.c`）⚠️ 待实现
- 单因素线性回归（最小二乘法）
- 模型评估（决定系数 R²、留出法 + RMSE）
- 多因子探索

### 模块五：系统集成（`ui.c` + `auth.c` + `main.c`）
- 用户登录（admin/guest）与权限控制
- 主菜单 + 子菜单循环
- 报告查看

---

## 六、重要约定

### 6.1 数据流
```
模块一加载 (raw CSV) → 模块二预处理 (清洗) → 模块三分析 → 模块四预测
                            ↓
                   data/processed/*.csv / *.bin  (处理后数据)
```

### 6.2 缺失值标记
数据加载时将以下情况统一标记为 `valid = false`，数值字段设为 `NaN`：
- 空字段（`,,`）
- 特殊数值（`-999`、`-9999`）
- NaN 文本（`NaN`、`nan`、`NAN`）

### 6.3 参数引用
对6个水质参数的操作应使用枚举 `ParamType` 进行统一迭代，避免为每个参数重复书写相似代码。可复用 `preprocess.c` 中的 `param_names[]`、`get_param_ptr()`、`get_param_value()` 等辅助函数模式。

### 6.4 时间序列基准
数据从 **2025年1月1日 12:00** 开始，采集频率为 **每5分钟一次**（每天288条记录）。时间筛选基于时间戳字符串中的时分部分（格式 `YYYY-MM-DD HH:MM`）。

### 6.5 输出文件
| 文件 | 内容 | 写入位置 |
|------|------|---------|
| `data_overview.txt` | 数据概览 + 预处理统计 | `reports/` |
| 预警报告 | 缺氧/盐度突变预警 | `reports/` |
| 统计分析报告 | 基本统计量 + 相关系数矩阵 | `reports/` |

### 6.6 构建约定
- 编译目标：`seawater_analysis`（不需 `.exe` 后缀，Makefile 已处理）
- 编译产物在 `build/` 目录，不提交
- 零警告编译：`-Wall` 下必须 0 warning

---

## 七、当前实现状态

| 模块 | 内容 | 状态 |
|------|------|------|
| 模块一 (1.1~1.7) | 数据基础操作 | ✅ 已完成 |
| 模块二 (2.1~2.3) | 数据预处理 | ✅ 已完成 |
| 模块三 (3.1~3.3) | 统计分析 | ❌ 空壳 |
| 模块四 (4.1~4.2) | 预测模型 | ❌ 空壳 |
| 模块五 (5.1~5.3) | 系统集成 | ⚠️ 部分（登录+菜单已完成；需串联模块三/四） |

---

## 八、添加新代码注意事项

1. 新增 `.c` 文件需同步更新 `Makefile`（当前 `SRC = $(wildcard $(SRCDIR)/*.c)` 已自动包含 `src/` 下所有 `.c` 文件）
2. 新增函数在头文件中声明，源文件中实现
3. 公开函数名保持动词开头的命名风格（`compute_`, `generate_`, `detect_`, `write_`）
4. 输出报告使用 `reports/` 目录，处理数据使用 `data/processed/` 目录
5. 涉及浮点数比较时使用容差（如 `fabs(a - b) < 1e-9`），不直接 `==` 比较
6. 用户可见的中文字符串确保在 UTF-8 编码下正常工作（`main.c` 已设控制台 UTF-8）
