# 修改内容声明 — 模块三

> **日期**：2026-06-15
> **模块**：模块三：统计分析（3.1 ~ 3.3）

---

## 一、修改概览

本次提交完成了《编程基础实践》任务书中**模块三：统计分析**的全部3个子任务（3.1~3.3）的代码实现、子菜单交互与文档编写。

模块三在整个系统中的位置：**数据消费层**——对模块一加载、模块二清洗后的数据进行统计分析和预警，为养殖管理决策提供数据支持。

---

## 二、修改文件清单

### 修改的已有文件（4个）

| 文件 | 修改内容 |
|------|---------|
| `include/analysis.h` | **重写全部内容**。新增 `compute_statistics`、`hypoxia_warning`、`salinity_warning`、`generate_correlation_matrix` 4个函数声明；移除旧空壳声明 |
| `src/analysis.c` | **重写全部内容**（原为4个空壳函数，共27行→约360行）。实现：基本统计量计算（均值/最值/标准差，6参数×4指标）、凌晨缺氧预警（按天分组，03:00-05:00）、盐度突变预警（1h变化率+24h累计降幅）、皮尔逊相关系数矩阵（6×6 + 极值 + 四组特定参数对分析） |
| `include/ui.h` | 新增 `show_analysis_submenu`、`handle_analysis_submenu` 函数声明 |
| `src/ui.c` | 主菜单 `case 3` 从单行 `printf` 改为子菜单循环（参照模块一/二模式）；新增 `show_analysis_submenu`、`handle_analysis_submenu` 两个函数（约95行）；新增 `#include "analysis.h"` |

### 新建文件（2个）

| 文件 | 说明 |
|------|------|
| `docs/module3_design.md` | 模块三设计论述文档：统计量选择依据、分段统计算法分析、皮尔逊相关系数计算方法 |
| `修改声明/修改内容声明_模块三.md` | 本文件，修改内容声明 |

---

## 三、功能覆盖情况（对照任务书）

### 3.1 基本统计量 ✅

**任务要求**：
- ✅ 计算每个参数的均值、最大值、最小值、标准差
- ✅ 统计结果写入统计分析报告（CSV格式）
- ✅ 样本仅取有效记录（`valid == true`）

**实现要点**：
- 一次遍历提取6个参数的有效值数组，O(n) 时间复杂度
- 统计函数（mean/max/min/stddev）独立封装，可复用
- 控制台 + CSV 文件双输出

### 3.2 分段统计 ✅

**凌晨缺氧预警**：
- ✅ 根据时间序列起止时间和5分钟采集频率筛选数据段
- ✅ 筛选每天 03:00-05:00 的 DO 数据求均值
- ✅ DO 均值 < 4.0 → 亚缺氧预警 + 建议开增氧机
- ✅ DO 均值 < 3.0 → 严重缺氧预警 + 建议投颗粒氧并减投喂
- ✅ 预警写入 `reports/warning_report.csv`

**盐度突变预警**：
- ✅ 1小时变化率：`|盐度(t) − 盐度(t−12)| > 2 PSU` → 预警
- ✅ 24小时累计降幅：日窗口 max−min > 5 PSU → 预警
- ✅ 预警写入 `reports/warning_report.csv`

### 3.3 相关性分析 ✅

**任务要求**：
- ✅ 实现皮尔逊相关系数计算函数
- ✅ 输出完整 6×6 相关系数矩阵
- ✅ 找出最强正相关和最强负相关
- ✅ 分析四组特定参数对：水温-DO、pH-DO、水温-气温、水温-盐度
- ✅ 矩阵及结论写入统计分析报告（CSV格式）

**实现要点**：
- 皮尔逊公式采用单遍累加：`r = (n·Σxy − Σx·Σy) / √[(n·Σx²−(Σx)²)·(n·Σy²−(Σy)²)]`
- 对角元强制为 1.0，方差为零返回 NAN
- 相关强度自动标注（极强/强/中等/弱/极弱）

---

## 四、代码质量

### 代码规模

| 文件 | 行数 | 说明 |
|------|------|------|
| `include/analysis.h` | 16行 | 头文件：4个函数声明 |
| `src/analysis.c` | ~360行 | 核心实现：3大功能 + 8个辅助函数 |
| `src/ui.c`（新增部分） | ~95行 | 子菜单 + 完整分析流程 |

### 编码规范

- ✅ 多文件组织（`.h` / `.c` 分离）
- ✅ 条件编译头文件保护（`#ifndef` / `#define` / `#endif`）
- ✅ 中文注释，与模块一/二风格一致
- ✅ 函数单一职责：统计、预警、相关分析各自独立
- ✅ 仅使用标准C库函数（`stdio.h`、`stdlib.h`、`string.h`、`math.h`）
- ✅ 内存管理：`malloc` 后有对应 `free`，异常路径提前释放
- ✅ NaN 安全处理：`isnan()` 检测，统计/矩阵计算中跳过

---

## 五、编译验证

```
gcc -std=c11 -Wall -Iinclude src/*.c -o seawater_analysis -lm
```
✅ 编译通过，零错误零警告。

---

## 六、输出文件说明

| 文件 | 格式 | 内容 |
|------|------|------|
| `reports/statistics_report.csv` | CSV | 基本统计量（6参数×4指标）+ 6×6相关系数矩阵 + 结论 |
| `reports/warning_report.csv` | CSV | 凌晨缺氧预警 + 盐度突变预警（时间/类型/等级/建议） |

两个文件均可用 Excel 直接打开查看。

---

## 七、与其他模块的关系

```
模块一（数据加载）
    ↓ 传入 WaterDataset*
模块二（数据预处理）
    ↓ 传出清洗后的 WaterDataset*
模块三（统计分析）← 本次实现
    ↓ 产出统计报告 + 预警报告
模块五（系统集成）
```

- **依赖模块一/二**：需要先加载数据并（可选）执行预处理
- **数据不修改**：所有分析均为只读操作，不改变内存中的 `WaterDataset`
- **模块四（预测模型）**：与模块三同为数据消费层，互不依赖，可独立执行
