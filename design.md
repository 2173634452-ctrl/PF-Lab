# 海水养殖水质数据分析系统 — 总体设计文档

> **项目名称**：海水养殖水质数据分析系统
> **语言**：C（C11 标准）
> **数据来源**：海南省陵水县新村镇海水养殖基地物联网监测系统
> **采集频率**：每 5 分钟 / 条，共 23,203 条记录
> **编制日期**：2026-06-15

---

## 一、系统概述

本系统对海水养殖基地的水质监测数据进行读取、清洗、统计分析、建模预测和结果展示，为养殖户提供水质预警和管理建议。

### 1.1 系统目标

| 目标 | 说明 |
|------|------|
| 数据管理 | 高效加载、存储、查询、修改海量时序数据（23,203 条） |
| 数据清洗 | 识别并修复异常值、缺失值，时间序列滤波降噪 |
| 风险预警 | 凌晨缺氧预警 + 盐度突变预警，输出可操作建议 |
| 统计分析 | 基本统计量 + 皮尔逊相关系数矩阵 |
| 预测建模 | 单因素线性回归，环境因子对 DO 的影响分析 |
| 安全控制 | 用户登录与权限管理（admin / guest） |

### 1.2 系统架构

```
┌─────────────────────────────────────────────────────┐
│                   main.c (程序入口)                    │
│                  auth.c (登录鉴权)                     │
│                   ui.c (菜单路由)                      │
├──────────┬──────────┬──────────┬──────────┬──────────┤
│ 模块一    │ 模块二    │ 模块三    │ 模块四    │ 模块五    │
│ data_io.c│preprocess│analysis.c│ model.c  │ 系统集成   │
│ backup.c │   .c     │          │          │           │
├──────────┼──────────┼──────────┼──────────┼──────────┤
│ 数据加载  │ 异常值    │ 基本统计量 │ 线性回归  │ 权限管理   │
│ 数据存储  │ 缺失值    │ 分段统计  │ 模型评估  │ 菜单交互   │
│ 数据查询  │ 移动平均  │ 相关分析  │ 多因子    │ 报告查看   │
│ 数据修改  │          │          │          │           │
│ 数据删除  │          │          │          │           │
│ 备份恢复  │          │          │          │           │
└──────────┴──────────┴──────────┴──────────┴──────────┘
```

---

## 二、数据流设计

### 2.1 数据生命周期

```
CSV 文件 (data/raw/)
    │
    ▼
[加载] load_csv_data()           ← 动态扩容、缺失值识别、时间戳生成
    │
    ▼
[预处理] detect_outliers()       ← 异常值检测（≥3 → 删除, <3 → 修复）
         fill_missing_values()   ← 均值逼近法填充
         moving_average()        ← 移动平均滤波（窗口 3/5/7/9/11）
    │
    ▼
[分析] compute_statistics()      ← 基本统计量（均值/最值/标准差）
       hypoxia_warning()         ← 凌晨缺氧预警（03:00-05:00）
       salinity_warning()        ← 盐度突变预警（1h + 24h）
       correlation_matrix()      ← 6×6 皮尔逊相关系数矩阵
    │
    ▼
[预测] linear_regression()       ← 最小二乘法拟合
       evaluate_model()          ← R² + 留出法 RMSE
    │
    ▼
[输出] reports/*.csv             ← 统计报告、预警报告
       data/processed/*.csv      ← 清洗后数据
       backup/*.csv              ← 带时间戳备份
```

### 2.2 核心数据结构

```c
typedef struct {
    char timestamp[32];       // 时间戳 "2025-01-01 12:00:00"
    double temp;              // 水温 (℃)
    double salinity;          // 盐度 (PSU)
    double pH;                // pH 值
    double DO;                // 溶解氧 (mg/L)
    double precipitation;     // 降水量 (m)
    double air_temp;          // 气温 (℃)
    bool valid;               // 数据有效性标记
} WaterRecord;

typedef struct {
    WaterRecord *records;     // 动态数组指针
    size_t total_count;       // 总记录数
    size_t valid_count;       // 有效记录数
    size_t capacity;          // 当前容量
    bool preprocessed;        // 是否已执行预处理
} WaterDataset;
```

### 2.3 参数合理范围

| 参数 | 最小值 | 最大值 | 单位 |
|------|--------|--------|------|
| 水温 (Temp) | -5 | 40 | ℃ |
| 盐度 (Salinity) | 0 | 45 | PSU |
| pH | 6.5 | 9.0 | — |
| 溶解氧 (DO) | 0 | 15 | mg/L |
| 降水量 (Precip) | 0 | 500 | m |
| 气温 (Air_temp) | -10 | 50 | ℃ |

---

## 三、模块设计要点

### 3.1 模块一：数据基础操作

| 设计点 | 决策 | 理由 |
|--------|------|------|
| 存储方式 | 动态数组 + 翻倍扩容 | 适应任意大小文件，内存峰值可控 |
| 初始容量 | 1000 条 | 小文件不浪费，大文件 5 次扩容至 32000 |
| CSV 解析 | 手动 `split_csv_line` | 处理缺失值（`,,`、`NaN`、`-999`）更灵活 |
| 时间戳 | 6 列自动生成 / 7 列读取 | 兼容有无时间戳列的两种数据格式 |
| 时间基准 | 2025-01-01 12:00 | 任务书 §3.2 明确要求 |
| 备份机制 | 操作前自动备份（时间戳命名） | 防误操作导致数据丢失 |
| 存储对比 | CSV（人类可读） vs 二进制（高效） | 不同场景择优使用 |

### 3.2 模块二：数据预处理

| 设计点 | 决策 | 理由 |
|--------|------|------|
| 异常值策略 | ≥3 异常 → 删除；<3 异常 → 均值逼近修复 | 任务书要求，平衡数据质量与时间序列连续性 |
| 扫描方式 | 两遍扫描（标记→处理） | 避免就地修改影响后续检测 |
| 缺失值填充 | 均值逼近法（n=m=10） | 利用时间序列局部相关性 |
| 边界处理 | 单方向无值→单向；双方向无值→全局均值 | 确保任何情况都有合理填充值 |
| 滤波对象 | Temp、DO、pH、Salinity | 降水量和气温变化较慢，不需要高频滤波 |
| 滤波窗口 | 3/5/7/9/11 交互式选择 | 用户可根据噪声程度自行选择 |
| 滤波边界 | 非对称窗口（可用点即除数） | 避免边缘信息丢失 |
| 预处理标记 | `preprocessed` 标志位 + 操作后重置 | 防止用户用脏数据做分析 |

### 3.3 模块三：统计分析

| 设计点 | 决策 | 理由 |
|--------|------|------|
| 统计量 | 单遍 O(n) 遍历 | 23,203 条数据可在一秒内完成 |
| 缺氧检测 | 按日历日期 + 小时提取 | 用 `extract_date()` + `extract_hour()` 精确定位 03:00-05:00 |
| 盐度 1h | 双指针滑动窗口 + 真实时间戳 | `timestamp_to_minutes()` 解析为 epoch 分钟，[55,65] 分钟容差 |
| 盐度 24h | 按日历日期分组 | `extract_date()` 分组，日极差阈值 > 5 PSU |
| 相关系数 | 单遍累加公式（避免两次遍历） | 数值稳定性良好（double 精度，23k 条不会溢出） |
| 对角元 | 强制 1.0 | 变量与自身完全相关 |
| 方差为零 | 返回 NaN | 常量序列无相关性 |
| 时间处理 | 全部基于真实时间戳 | 不依赖固定索引，适应删除/修改后的数据 |

### 3.4 模块五：系统集成

| 设计点 | 决策 | 理由 |
|--------|------|------|
| 用户角色 | admin（全权限）/ guest（只读） | 任务书 §5.1 要求 |
| 登录限制 | 3 次尝试后退出 | 防暴力破解 |
| 菜单结构 | 主菜单 + 子菜单循环 | 清晰的模块导航 |
| 预处理检查 | 分析前 Y/N 提醒 | 引导用户先清洗数据 |
| 未加载检查 | `ensure_data_loaded` 守卫 | 空数据集下操作有友好提示 |

---

## 四、关键算法详解

### 4.1 时间戳自动生成

```c
// 从 2025-01-01 12:00 开始，每条记录 +5 分钟
int total_minutes = (int)dataset->total_count * 5;
struct tm base = {0};
base.tm_year = 2025 - 1900;
base.tm_mon  = 0;            // January
base.tm_mday = 1;
base.tm_hour = 12;           // 12:00 起始（任务书 §3.2）
base.tm_min  = 0;
base.tm_sec  = 0;
time_t t = mktime(&base) + total_minutes * 60;
struct tm *local = localtime(&t);
strftime(rec->timestamp, sizeof(rec->timestamp), "%Y-%m-%d %H:%M:%S", local);
```

### 4.2 盐度突变 1 小时 — 双指针滑动窗口

```
外层循环 i 扫描每条记录
  │
  ├─ t_cur = timestamp_to_minutes(records[i])
  │
  └─ 内层循环 prev 追赶（双指针，不回退）
       │
       ├─ t_prev = timestamp_to_minutes(records[prev])
       ├─ gap = t_cur - t_prev
       │
       ├─ gap > 65  → prev++（太早，追赶）
       ├─ gap < 55  → break （太近，停止）
       └─ gap ∈ [55,65] → 比较盐度差 > 2 PSU？
            └─ prev++, break
```

**时间复杂度**：O(n)，每个记录最多被 `prev` 访问一次（单调不递减）。
**鲁棒性**：不依赖 12 条固定偏移，适应任意增删操作。

### 4.3 缺氧预警 — 按日历日期分组

```
while i < total_count:
    date = extract_date(records[i])
    while 同日期:
        if hour ∈ [3, 4] (即 03:00-04:55):
            收集 DO 值
        i++
    计算当日凌晨 DO 均值 → 分级预警
```

### 4.4 皮尔逊相关系数 — 单遍累加

```
r = (n·Σxy − Σx·Σy) / √[(n·Σx²−(Σx)²) · (n·Σy²−(Σy)²)]
```

一次遍历同时累加 `Σx`, `Σy`, `Σxy`, `Σx²`, `Σy²`，无需存储全量数据。

---

## 五、BUG 修复历史

本系统在迭代开发过程中发现并修复了以下关键 BUG：

### 5.1 CSV 列数不匹配（BUG-1）🔴

| 项目 | 说明 |
|------|------|
| **发现时间** | 模块三集成测试 |
| **现象** | 数据浏览显示所有记录参数均为 0.0 |
| **根因** | CSV 为 6 列（无时间戳），解析器期望 7 列，`field_count < 7` 过滤所有行 |
| **修复** | 改为 `field_count < 6`，增加 `offset` 变量适配两种列数格式 |
| **文件** | `src/data_io.c` |

### 5.2 时间戳溢出（BUG-2）🟡

| 项目 | 说明 |
|------|------|
| **发现时间** | 模块三分析输出 |
| **现象** | 后期记录时间戳显示 `2024-01-81`、`2024-01-99` 等非法日期 |
| **根因** | 用 `sprintf("2024-01-%02d", day_index)` 硬编码拼凑，日索引 > 31 时溢出 |
| **修复** | 改用 `mktime()` + `strftime()` 标准日历函数；基准改为 2025-01-01 12:00 |
| **文件** | `src/data_io.c` |

### 5.3 盐度 1h 固定索引（BUG-3）🟡

| 项目 | 说明 |
|------|------|
| **发现时间** | 盐度突变测试 |
| **现象** | 删除记录后，`records[i]` 与 `records[i-12]` 的时间差不再为 1 小时 |
| **根因** | 用固定 12 条索引偏移代替 1 小时时间差 |
| **修复** | 双指针滑动窗口 + `timestamp_to_minutes()` 真实时间比较 |
| **文件** | `src/analysis.c` |

### 5.4 盐度 24h 固定批次（BUG-4）🟡

| 项目 | 说明 |
|------|------|
| **发现时间** | 盐度突变测试 |
| **现象** | 删除记录后，288 条批次边界与日历日错位 |
| **根因** | 用固定 288 条批次代替日历日分组 |
| **修复** | 改用 `extract_date()` 按 YYYY-MM-DD 分组 |
| **文件** | `src/analysis.c` |

### 5.5 缺少预处理检查（BUG-5）🟢

| 项目 | 说明 |
|------|------|
| **发现时间** | 功能审查 |
| **现象** | 用户可在未预处理的情况下直接分析，结果包含异常值干扰 |
| **根因** | 分析模块无前置条件检查 |
| **修复** | `WaterDataset.preprocessed` 标志位 + 分析前 Y/N 确认 |
| **文件** | `include/common.h`, `src/ui.c`, `src/data_io.c` |

### 5.6 预处理标记不一致（BUG-6）🟢

| 项目 | 说明 |
|------|------|
| **发现时间** | 功能审查 |
| **现象** | 修改/删除记录后系统仍显示"预处理已完成" |
| **根因** | 数据变更函数未重置 `preprocessed` 标志 |
| **修复** | `modify_single_record`、`delete_single_record`、`batch_delete_records` 执行后重置为 `false` |
| **文件** | `src/data_io.c` |

### BUG 修复时间线

```
模块一完成 → 模块二完成 → 模块三完成
                              │
               ┌──────────────┼──────────────┐
               ▼              ▼              ▼
            BUG-1          BUG-2          BUG-3
           数据显示全0      日期溢出        盐度1h索引
                              │
               ┌──────────────┼──────────────┐
               ▼              ▼              ▼
            BUG-4          BUG-5          BUG-6
           盐度24h批次     缺少预处理检查    标记不一致
```

---

## 六、文件组织

```
project1/
├── include/            # 头文件（9 个）
│   ├── common.h        #   核心数据结构、参数范围常量
│   ├── data_io.h       #   数据加载、存储、查询、修改、删除
│   ├── preprocess.h    #   异常值检测、缺失值填充、移动平均滤波
│   ├── analysis.h      #   统计量、缺氧预警、盐度预警、相关分析
│   ├── model.h         #   线性回归预测
│   ├── auth.h          #   用户登录、权限管理
│   ├── backup.h        #   数据备份、恢复
│   ├── ui.h            #   菜单界面、交互逻辑
│   └── utils.h         #   通用工具函数
├── src/                # 源文件（9 个，与头文件一一对应）
├── docs/               # 设计论述文档
│   ├── module1_design.md   #   动态数组选型、CSV vs 二进制对比
│   ├── module2_design.md   #   异常值阈值、均值逼近法、滤波窗口分析
│   └── module3_design.md   #   统计量、分段统计、相关系数算法
├── 修改声明/           # 每次提交的修改内容声明
├── data/               # 数据文件
│   ├── raw/            #   原始 CSV 数据
│   └── processed/      #   预处理后的输出
├── backup/             # 自动备份文件
├── reports/            # 分析报告输出
└── 项目规范.md          # 编码规范与构建约定
```

---

## 七、构建与运行

```bash
# 编译（需 GCC / MinGW）
gcc -std=c11 -Wall -Iinclude src/*.c -o seawater_analysis -lm

# 运行
./seawater_analysis
```

**编译要求**：C11 标准，`-lm` 链接数学库，零警告。

---

## 八、设计原则总结

1. **时间戳永远是真相来源**：所有时序分析基于真实时间戳字符串，不依赖数组索引推算时间
2. **删除后时间不错位**：时间戳存储在每条记录中，记录删除不影响其他记录的时间语义
3. **预处理是分析的守门人**：`preprocessed` 标志位确保用户知晓数据清洗状态
4. **动态内存灵活适应**：翻倍扩容策略平衡内存峰值与分配次数
5. **函数单一职责**：每个函数只做一件事，降低耦合，便于测试和修复
6. **数据只读分析**：分析函数不修改数据集，分析结果写入独立报告文件
