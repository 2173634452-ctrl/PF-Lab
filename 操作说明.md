# 海水养殖水质分析系统 — 操作说明

> 版本：v1.0 | 日期：2026-05-31

---

## 一、启动程序

双击 `seawater_analysis.exe`，首先进入登录界面：

```
用户名: admin
密码: 123456
```

系统预设两个账户：

| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| `admin` | `123456` | 管理员 | 所有功能 |
| `guest` | `guest` | 访客 | 仅查看报告 |

> 最多输错3次，超过则程序自动退出。

---

## 二、主菜单

登录成功后显示主菜单：

```
========================================
  海水养殖水质分析系统 v1.0
========================================
 [1] 数据基础操作
 [2] 数据预处理
 [3] 统计分析
 [4] 预测分析
 [5] 查看数据概览
 [6] 查看预警报告
 [7] 查看分析报告
 [8] 数据备份与恢复
 [9] 清屏
 [0] 退出系统
========================================
  请选择操作 (0-9):
```

输入数字回车即可。目前**模块一（选项1）已完整实现**，其余模块待后续开发。

---

## 三、模块一：数据基础操作

在主菜单输入 `1` 进入子菜单：

```
  ╔════════════════════════════════════╗
  ║      模块一：数据基础操作          ║
  ╠════════════════════════════════════╣
  ║  [1] 加载CSV数据                  ║
  ║  [2] 分页浏览数据                 ║
  ║  [3] 按条件筛选                   ║
  ║  [4] 按参数排序                   ║
  ║  [5] 修改单条记录                 ║
  ║  [6] 删除单条记录                 ║
  ║  [7] 批量删除记录                 ║
  ║  [8] 保存数据(CSV)                ║
  ║  [9] 保存数据(二进制)             ║
  ║ [10] 存储性能对比                 ║
  ║ [11] 手动备份数据                 ║
  ║ [12] 从备份恢复数据               ║
  ║  [0] 返回主菜单                   ║
  ╚════════════════════════════════════╝
  当前数据: 未加载
  请选择操作 (0-12):
```

### 3.1 加载CSV数据 — `[1]`

**第一步必须执行此操作！** 输入CSV文件路径：

```
请输入CSV文件路径 (默认: data/raw/data.csv):
```

- 直接回车 → 使用默认路径 `data/raw/data.csv`
- 或输入自定义路径，如 `D:\mydata.csv`

加载成功后会显示：
```
读取完成：总记录 23203，有效记录 22000（94.8%）
数据概览已写入 reports/data_overview.txt
```

---

### 3.2 分页浏览数据 — `[2]`

```
╔══════════════════════════════════════════════════════════════╗
║  数据浏览 — 第 1 / 1547 页（共 23203 条记录）                 ║
╠══════════════════════════════════════════════════════════════╣
║ 序号 时间                 水温   盐度    pH     DO   降水   气温 ║
╠══════════════════════════════════════════════════════════════╣
║ 1    2025-01-01 12:00    25.6  34.0  8.15  6.80  0.001  27.8 ║
║ 2    2025-01-01 12:05    25.6  34.0  8.14  6.81  0.001  27.8 ║
║ ...                                                          ║
╚══════════════════════════════════════════════════════════════╝
  [N]下一页  [P]上一页  [J]跳转  [Q]返回:
```

- **N** → 下一页
- **P** → 上一页
- **J** → 跳转到指定页（输入页码）
- **Q** → 返回子菜单

> 每页显示15条，`*` 标记表示该条含缺失数据。

---

### 3.3 按条件筛选 — `[3]`

```
可筛选参数: temp / salinity / pH / DO / precipitation / air_temp
请输入参数名: DO
请输入最小值: 3
请输入最大值: 5
```

输出所有溶解氧在 3~5 mg/L 范围内的记录（最多显示前100条）。

---

### 3.4 按参数排序 — `[4]`

```
可排序参数:
  [0] 水温     [1] 盐度     [2] pH
  [3] 溶解氧   [4] 降水量   [5] 气温
请选择参数编号 (0-5): 3
排序方向: [A]升序  [D]降序: A
```

按溶解氧从低到高排列，显示前50条。

---

### 3.5 修改单条记录 — `[5]`

```
请输入要修改的记录号 (1-23203): 100

当前记录 #100:
  时间: 2025-01-01 20:15
  [1] 水温:      25.80 ℃
  [2] 盐度:      34.20 PSU
  [3] pH:         8.12
  [4] 溶解氧:     7.20 mg/L
  [5] 降水量:     0.0000 m
  [6] 气温:      28.10 ℃

请选择要修改的参数 (1-6): 4
请输入新值: 7.5
修改前自动备份原始数据...
备份成功，文件已保存至 backup/backup_20260531_143025.csv
修改成功！
是否将修改后的数据保存到文件？(y/n): y
保存成功！
```

- 新值超出合理范围会自动拒绝
- 修改前自动备份到 `backup/` 目录
- 修改后可选择保存到文件

---

### 3.6 删除单条记录 — `[6]`

```
请输入要删除的记录号 (1-23203): 500
待删除记录 #500:
  时间: ... | 水温:... 盐度:... pH:... DO:... 降水:... 气温:...
确认删除该记录？此操作不可撤销！(y/n): y
删除前自动备份原始数据...
删除成功！当前总记录数: 23202
```

---

### 3.7 批量删除 — `[7]`

```
可筛选参数: temp / salinity / pH / DO / precipitation / air_temp
请输入参数名: DO
请输入最小值: 20
请输入最大值: 100
找到 15 条匹配记录。
确认批量删除这些记录？此操作不可撤销！(y/n): y
删除前自动备份原始数据...
批量删除成功！删除了 15 条记录，当前总记录数: 23187
```

---

### 3.8 保存数据 — `[8]` / `[9]`

- `[8]` 保存为CSV文本格式（可用Excel打开）
- `[9]` 保存为二进制格式（体积小、读写快）

```
请输入保存路径 (默认: data/processed/cleaned_data.csv):
CSV数据保存成功 → data/processed/cleaned_data.csv
```

---

### 3.9 存储性能对比 — `[10]`

自动生成CSV vs 二进制的性能对比表：

```
╔══════════════════════════════════════════════════════════════╗
║              存储格式性能对比                                ║
╠═══════════╤══════════════╤════════════╤═══════════╤════════╣
║ 存储格式  │ 文件大小(B)  │ 写入时间(s) │ 读取时间(s)│ 可读  ║
╟───────────┼──────────────┼────────────┼───────────┼────────╢
║ CSV文本   │ 1520000      │ 0.0450     │ 0.0320    │  是   ║
║ 二进制    │ 450000       │ 0.0020     │ 0.0010    │  否   ║
╚═══════════╧══════════════╧════════════╧═══════════╧════════╝
```

---

### 3.10 手动备份 — `[11]`

```
备份成功，文件已保存至 backup/backup_20260531_150230.csv
```

备份文件名为 `backup_YYYYMMDD_HHMMSS.csv`，带精确时间戳，不会覆盖旧备份。

---

### 3.11 从备份恢复 — `[12]`

```
可用的备份文件:
  [1] backup/backup_20260531_143025.csv
  [2] backup/backup_20260531_150230.csv
请选择要恢复的备份编号 (1-2, 0取消): 1
恢复数据将覆盖当前内存中的数据，确认？(y/n): y
格式验证通过：23203 行数据，7 列。
恢复成功！从 backup/... 加载了 23203 条记录。
```

---

## 四、快捷操作（主菜单）

| 选项 | 功能 |
|------|------|
| `[5]` | 查看数据概览（总记录数、有效记录数、有效率） |
| `[8]` | 直接备份/恢复（无需进入模块一子菜单） |
| `[9]` | 清屏 |
| `[0]` | 退出系统（有确认提示） |

---

## 五、典型操作流程

```
1. 启动 → 登录 (admin / 123456)
2. 按 [1] → 进入模块一子菜单
3. 按 [1] → 输入文件路径，加载 data.csv
4. 按 [2] → 分页浏览数据
5. 按 [3] → 筛选 DO < 4.0 的低氧记录
6. 按 [5] → 修改异常值
7. 按 [8] → 保存清洗后的数据
8. 按 [0] → 返回主菜单
9. 按 [0] → 退出系统
```

---

## 六、注意事项

1. **数据文件**：程序依赖 `data/raw/data.csv`，请确保该文件存在
2. **备份自动触发**：修改或删除数据前会自动备份，备份文件位于 `backup/` 目录
3. **删除不可撤销**：所有删除操作都有二次确认，请仔细核对
4. **参数范围验证**：修改数据时新值必须在合理范围内，否则会被拒绝
5. **编码**：程序启动时自动切换控制台为 UTF-8，中文正常显示
