# 海水养殖水质分析系统 — 操作说明

> 版本：v1.2 | 日期：2026-06-18

---

## 一、启动程序

双击 `seawater_analysis.exe`，首先进入登录界面：

```
用户名: admin
密码: 123456
```

系统预设两个账户：

| 用户名 | 密码 | 角色 | 权限 |
|--------|------|------|------|
| `admin` | `123456` | 管理员 | 所有功能 |
| `guest` | `guest` | 访客 | 仅查看报告、清屏、退出 |

> 最多输错3次，超过则程序自动退出。
> **权限说明**：访客仅可使用 [5] 数据概览、[7] 分析报告、[9] 清屏、[0] 退出。主菜单会根据角色动态显示可用选项。

---

## 二、主菜单

登录成功后显示主菜单：

```
========================================
  海水养殖水质分析系统 v1.0
========================================
 [1] 数据基础操作
 [2] 数据预处理
 [3] 统计分析
 [4] 预测分析
 [5] 查看数据概览
 [6] 查看预警报告
 [7] 查看分析报告
 [8] 数据备份与恢复
 [9] 清屏
 [0] 退出系统
========================================
  请选择操作 (0-9):
```

输入数字回车即可。**模块一（选项1）和模块二（选项2）已完整实现**，其余模块待后续开发。

---

## 三、模块一：数据基础操作

在主菜单输入 `1` 进入子菜单：

```
  ╔════════════════════════════════════╗
  ║      模块一：数据基础操作          ║
  ╠════════════════════════════════════╣
  ║  [1] 加载CSV数据                  ║
  ║  [2] 分页浏览数据                 ║
  ║  [3] 按条件筛选                   ║
  ║  [4] 按参数排序                   ║
  ║  [5] 修改单条记录                 ║
  ║  [6] 删除单条记录                 ║
  ║  [7] 批量删除记录                 ║
  ║  [8] 保存数据(CSV)                ║
  ║  [9] 保存数据(二进制)             ║
  ║ [10] 存储性能对比                 ║
  ║ [11] 手动备份数据                 ║
  ║ [12] 从备份恢复数据               ║
  ║  [0] 返回主菜单                   ║
  ╚════════════════════════════════════╝
  当前数据: 未加载
  请选择操作 (0-12):
```

### 3.1 加载CSV数据 — `[1]`

**第一步必须执行此操作！** 输入CSV文件路径：

```
请输入CSV文件路径 (默认: data/raw/data.csv):
```

- 直接回车 → 使用默认路径 `data/raw/data.csv`
- 或输入自定义路径，如 `D:\mydata.csv`

加载成功后会显示：
```
读取完成：总记录 23203，有效记录 22000（94.8%）
数据概览已写入 reports/data_overview.txt
```

---

### 3.2 分页浏览数据 — `[2]`

```
╔══════════════════════════════════════════════════════════════╗
║  数据浏览 — 第 1 / 1547 页（共 23203 条记录）                 ║
╠══════════════════════════════════════════════════════════════╣
║ 序号 时间                 水温   盐度    pH     DO   降水   气温 ║
╠══════════════════════════════════════════════════════════════╣
║ 1    2025-01-01 12:00    25.6  34.0  8.15  6.80  0.001  27.8 ║
║ 2    2025-01-01 12:05    25.6  34.0  8.14  6.81  0.001  27.8 ║
║ ...                                                          ║
╚══════════════════════════════════════════════════════════════╝
  [N]下一页  [P]上一页  [J]跳转  [Q]返回:
```

- **N** → 下一页
- **P** → 上一页
- **J** → 跳转到指定页（输入页码）
- **Q** → 返回子菜单

> 每页显示15条，`*` 标记表示该条含缺失数据。

---

### 3.3 按条件筛选 — `[3]`

```
可筛选参数: temp / salinity / pH / DO / precipitation / air_temp
请输入参数名: DO
请输入最小值: 3
请输入最大值: 5
```

输出所有溶解氧在 3~5 mg/L 范围内的记录（最多显示前100条）。

---

### 3.4 按参数排序 — `[4]`

```
可排序参数:
  [0] 水温     [1] 盐度     [2] pH
  [3] 溶解氧   [4] 降水量   [5] 气温
请选择参数编号 (0-5): 3
排序方向: [A]升序  [D]降序: A
```

按溶解氧从低到高排列，显示前50条。

---

### 3.5 修改单条记录 — `[5]`

```
请输入要修改的记录号 (1-23203): 100

当前记录 #100:
  时间: 2025-01-01 20:15
  [1] 水温:      25.80 ℃
  [2] 盐度:      34.20 PSU
  [3] pH:         8.12
  [4] 溶解氧:     7.20 mg/L
  [5] 降水量:     0.0000 m
  [6] 气温:      28.10 ℃

请选择要修改的参数 (1-6): 4
请输入新值: 7.5
修改前自动备份原始数据...
备份成功，文件已保存至 backup/backup_20260531_143025.csv
修改成功！
是否将修改后的数据保存到文件？(y/n): y
保存成功！
```

- 新值超出合理范围会自动拒绝
- 修改前自动备份到 `backup/` 目录
- 修改后可选择保存到文件

---

### 3.6 删除单条记录 — `[6]`

```
请输入要删除的记录号 (1-23203): 500
待删除记录 #500:
  时间: ... | 水温:... 盐度:... pH:... DO:... 降水:... 气温:...
确认删除该记录？此操作不可撤销！(y/n): y
删除前自动备份原始数据...
删除成功！当前总记录数: 23202
```

---

### 3.7 批量删除 — `[7]`

```
可筛选参数: temp / salinity / pH / DO / precipitation / air_temp
请输入参数名: DO
请输入最小值: 20
请输入最大值: 100
找到 15 条匹配记录。
确认批量删除这些记录？此操作不可撤销！(y/n): y
删除前自动备份原始数据...
批量删除成功！删除了 15 条记录，当前总记录数: 23187
```

---

### 3.8 保存数据 — `[8]` / `[9]`

- `[8]` 保存为CSV文本格式（可用Excel打开）
- `[9]` 保存为二进制格式（体积小、读写快）

```
请输入保存路径 (默认: data/processed/cleaned_data.csv):
CSV数据保存成功 → data/processed/cleaned_data.csv
```

---

### 3.9 存储性能对比 — `[10]`

自动生成CSV vs 二进制的性能对比表：

```
╔══════════════════════════════════════════════════════════════╗
║              存储格式性能对比                                ║
╠═══════════╤══════════════╤════════════╤═══════════╤════════╣
║ 存储格式  │ 文件大小(B)  │ 写入时间(s) │ 读取时间(s)│ 可读  ║
╟───────────┼──────────────┼────────────┼───────────┼────────╢
║ CSV文本   │ 1520000      │ 0.0450     │ 0.0320    │  是   ║
║ 二进制    │ 450000       │ 0.0020     │ 0.0010    │  否   ║
╚═══════════╧══════════════╧════════════╧═══════════╧════════╝
```

---

### 3.10 手动备份 — `[11]`

```
备份成功，文件已保存至 backup/backup_20260531_150230.csv
```

备份文件名为 `backup_YYYYMMDD_HHMMSS.csv`，带精确时间戳，不会覆盖旧备份。

---

### 3.11 从备份恢复 — `[12]`

```
可用的备份文件:
  [1] backup/backup_20260531_143025.csv
  [2] backup/backup_20260531_150230.csv
请选择要恢复的备份编号 (1-2, 0取消): 1
恢复数据将覆盖当前内存中的数据，确认？(y/n): y
格式验证通过：23203 行数据，7 列。
恢复成功！从 backup/... 加载了 23203 条记录。
```

---

## 四、模块二：数据预处理

> **重要**：使用模块二之前，必须先在模块一中加载数据（[1] → [1] 加载CSV文件）。
> 建议操作顺序：先检测异常值 → 再处理缺失值 → 最后移动平均滤波。

在主菜单输入 `2` 进入子菜单：

```
  ╔════════════════════════════════════╗
  ║      模块二：数据预处理            ║
  ╠════════════════════════════════════╣
  ║  [1] 检测与处理异常值 (2.1)       ║
  ║  [2] 缺失值处理 (2.2)             ║
  ║  [3] 移动平均滤波 (2.3)           ║
  ║  [4] 保存处理后数据(CSV)          ║
  ║  [5] 保存处理后数据(二进制)       ║
  ║  [6] 查看预处理统计               ║
  ║  [0] 返回主菜单                   ║
  ╚════════════════════════════════════╝
  当前数据: 已加载
  请选择操作 (0-6):
```

---

### 4.1 检测与处理异常值 — `[1]`

**功能**：扫描所有数据，找出超出合理范围的异常值，自动修复或删除。

**合理范围表**：

| 参数 | 最小值 | 最大值 | 单位 |
|------|--------|--------|------|
| 水温 | -5 | 40 | ℃ |
| 盐度 | 0 | 45 | PSU |
| pH | 6.5 | 9.0 | — |
| 溶解氧 | 0 | 15 | mg/L |
| 降水量 | 0 | 500 | m |
| 气温 | -10 | 50 | ℃ |

**处理规则**：
- 一条记录中异常参数 **≥ 3个** → 整条记录删除
- 一条记录中异常参数 **< 3个** → 用均值逼近法自动填充异常值

**运行示例**：

```
╔══════════════════════════════════════════════════════════╗
║           2.1 异常值检测与处理                           ║
╚══════════════════════════════════════════════════════════╝

正在扫描异常值...

┌──────────────────────────────────────────────────────────┐
│              异常值检测概览                               │
├──────────────────────────────────────────────────────────┤
│  总记录数:              23203                            │
│  有效记录数:            21500                            │
│  异常数据记录数:          350                            │
│  异常数据参数个数:        520                            │
│  异常数据时间跨度:   2025-01-15 08:30 ~ 2025-03-20 14:15 │
└──────────────────────────────────────────────────────────┘

处理策略：
  异常参数 ≥ 3 的记录: 45 条 → 将整条删除
  异常参数 < 3 的记录: 305 条 → 将用均值逼近法填充

┌──────────────────────────────────────────────────────────┐
│              异常值处理结果                               │
├──────────────────────────────────────────────────────────┤
│  修复异常值记录数:      305                            │
│  删除异常值记录数:       45                            │
│  处理后总记录数:      23158                            │
│  处理后有效记录数:    23158                            │
└──────────────────────────────────────────────────────────┘
预处理统计信息已追加写入 reports/data_overview.txt
```

---

### 4.2 缺失值处理（均值逼近法） — `[2]`

**功能**：自动检测并填补数据中的空值、NaN、-999等缺失标记。

**填充公式**：\(a_i = \dfrac{a_{i-n} + a_{i+m}}{2}\)

- \(n = m = 10\)（向前/向后各找第10个有效数据点）
- 如果某方向找不到有效值 → 用另一方向的值
- 如果两个方向都找不到 → 用该参数的全局均值

**运行示例**：

```
╔══════════════════════════════════════════════════════════╗
║         2.2 缺失值处理——均值逼近法                      ║
╚══════════════════════════════════════════════════════════╝

检测到 1703 条包含缺失值的记录。
正在使用均值逼近法填充（n=m=10）...

┌──────────────────────────────────────────────────────────┐
│              缺失值处理结果                               │
├──────────────────────────────────────────────────────────┤
│  处理的缺失值个数:     3200                            │
│  处理后总记录数:      23158                            │
│  处理后有效记录数:    23158                            │
└──────────────────────────────────────────────────────────┘
预处理统计信息已追加写入 reports/data_overview.txt
```

---

### 4.3 移动平均滤波 — `[3]`

**功能**：对水温、溶解氧、pH、盐度四个核心参数进行平滑滤波，去除传感器噪声。

**窗口选择**：

```
╔══════════════════════════════════════════════════════════╗
║         2.3 移动平均滤波 — 窗口选择                     ║
╠══════════════════════════════════════════════════════════╣
║  可选窗口: 3 / 5 / 7 / 9 / 11                          ║
║  窗口越大，曲线越平滑，但可能丢失短期变化细节          ║
╚══════════════════════════════════════════════════════════╝

请输入窗口大小: 5
```

**滤波效果对比**（窗口=5）：

```
╔══════════════════════════════════════════════════════════╗
║         2.3 移动平均滤波（窗口 = 5）                    ║
╚══════════════════════════════════════════════════════════╝

  水温(Temp)          滤波前标准差: 1.2345  →  滤波后标准差: 0.8765  (噪声降低 29.0%)
  溶解氧(DO)         滤波前标准差: 2.3456  →  滤波后标准差: 1.5432  (噪声降低 34.2%)
  pH                    滤波前标准差: 0.2345  →  滤波后标准差: 0.1543  (噪声降低 34.2%)
  盐度(Salinity)      滤波前标准差: 1.8765  →  滤波后标准差: 1.2345  (噪声降低 34.2%)

移动平均滤波完成（窗口 = 5）。
```

**分析讨论**（程序自动显示）：

```
┌──────────────────────────────────────────────────────────┐
│  分析讨论：窗口大小与噪声抑制的关系                      │
├──────────────────────────────────────────────────────────┤
│  窗口=3  → 轻度平滑，保留短期波动                       │
│  窗口=5  → 中度平滑，滤除高频噪声                       │
│  窗口=7  → 较强平滑，趋势更明显                         │
│  窗口=9  → 强平滑，适合分析长期趋势                     │
│  窗口=11 → 最强平滑，可能丢失短期变化                   │
│                                                          │
│  建议：可多次尝试不同窗口，观察标准差降低程度。          │
│  通常窗口5或7能在平滑度和细节保留之间取得较好平衡。      │
└──────────────────────────────────────────────────────────┘
```

**操作提示**：可以反复执行 `[3]` 尝试不同窗口大小，对比滤波效果。每次滤波都会直接修改内存中的数据。

---

### 4.4 保存处理后数据 — `[4]` / `[5]`

将预处理后的干净数据保存到文件：

- `[4]` 保存为CSV文本格式（推荐，可用Excel打开查看）
- `[5]` 保存为二进制格式（体积更小，读写更快）

```
请输入保存路径 (默认: data/processed/preprocessed_data.csv):
预处理数据已保存至 data/processed/preprocessed_data.csv
```

---

### 4.5 查看预处理统计 — `[6]`

随时查看当前内存中数据经过了哪些预处理操作：

```
┌──────────────────────────────────────────────────────────┐
│              预处理统计信息                               │
├──────────────────────────────────────────────────────────┤
│  [异常值检测]                                            │
│    异常数据记录数:          350                          │
│    异常参数总个数:          520                          │
│    异常时间跨度:       2025-01-15 08:30 ~ 2025-03-20     │
│    修复异常值记录数:        305                          │
│    删除异常值记录数:         45                          │
│  [缺失值处理]                                            │
│    处理的缺失值个数:       3200                          │
└──────────────────────────────────────────────────────────┘
```

> 以上统计数据也会自动追加写入 `reports/data_overview.txt` 文件，方便离线查看。

---

## 五、快捷操作（主菜单）

| 选项 | 功能 |
|------|------|
| `[5]` | 查看数据概览（含预处理统计信息） |
| `[8]` | 直接备份/恢复（无需进入模块一子菜单） |
| `[9]` | 清屏 |
| `[0]` | 退出系统（有确认提示） |

---

## 六、典型操作流程

### 完整数据处理流程

```
 1. 启动 → 登录 (admin / 123456)
 2. 按 [1] → 进入模块一子菜单
 3. 按 [1] → 输入文件路径，加载数据文件
 4. 按 [0] → 返回主菜单
 5. 按 [2] → 进入模块二子菜单
 6. 按 [1] → 检测并处理异常值（自动修复/删除）
 7. 按 [2] → 缺失值填充（均值逼近法）
 8. 按 [3] → 输入 5 → 移动平均滤波（窗口=5）
 9. 按 [6] → 查看预处理统计，确认处理结果
10. 按 [4] → 保存预处理后的数据为CSV
11. 按 [0] → 返回主菜单
12. 按 [5] → 查看完整数据概览（含预处理统计）
13. 按 [0] → 退出系统
```

### 快速查看数据质量

```
1. 加载数据 → 进入模块二
2. 按 [1] → 查看有多少异常值（先不处理，看统计数字即可）
3. 按 [6] → 查看详细统计
4. 确认数据质量后，再决定是否需要处理
```

---

## 七、注意事项

1. **数据文件**：程序依赖CSV数据文件（默认路径 `data/raw/data.csv`），请确保该文件存在且格式正确（7列：时间戳 + 6个参数）
2. **操作顺序**：建议按 **异常值 → 缺失值 → 滤波** 的顺序操作。前一步的输出是后一步的输入
3. **滤波不可逆**：移动平均滤波直接修改内存数据，建议在滤波前先手动备份（模块一 `[11]`）
4. **备份自动触发**：模块一中的修改或删除操作会自动备份；模块二的预处理不会自动备份（因为这是预期的大批量数据清洗操作）
5. **统计信息持久化**：所有预处理统计信息自动追加写入 `reports/data_overview.txt`
6. **删除不可撤销**：所有删除操作都有二次确认，请仔细核对
7. **参数范围验证**：修改数据时新值必须在合理范围内，否则会被拒绝
8. **编码**：程序启动时自动切换控制台为 UTF-8，中文正常显示
9. **窗口大小选择**：不确定选哪个窗口时，先用5试试——它通常在平滑度和细节保留之间取得较好平衡

---

## 八、v1.2 修复记录 (2026-06-18)

本次更新修复了模块一至三及模块五中的 8 个问题。详细说明见 `修改声明/修改内容声明_Bug修复.md`。

### 修复的关键问题

| # | 问题 | 影响 | 修复 |
|---|------|------|------|
| 1 | 缺失值加载后置为 0.0 而非 NaN | 缺失值填充完全不生效 | 解析失败时字段显式设为 NAN |
| 2 | 异常检测跳过含缺失值的记录 | 含缺失+异常值的记录异常永不被发现 | 扫描全部记录，NaN不计为异常 |
| 3 | save_csv_data 只保存有效记录 | 用户保存后无效记录丢失 | 无效记录输出 NaN 占位 |
| 4 | 权限控制未生效 | 访客可访问全部功能 | 菜单根据角色动态显示并校验权限 |
| 5 | Windows 下 mkdir 编译兼容 | 部分 MinGW 版本编译失败 | 改用 `_mkdir` |
| 6 | CSV 行尾 `\r\n` 处理不完整 | 跨平台时尾字段可能被 `\r` 污染 | while循环清除所有行尾换行符 |
| 7 | 退出确认双重打印提示 | 用户看到两条 (y/n) 提示 | 删除冗余 printf，统一用 confirm_action |
| 8 | 输入错误时清屏顺序不当 | 错误提示一闪而过用户看不到 | 先显示错误，再清屏重绘菜单 |

### 推荐的处理顺序

修复后，应按以下顺序执行模块二：

```
模块一加载数据 → 异常值检测(2.1) → 缺失值填充(2.2) → 移动平均滤波(2.3) → 模块三分析
```

两个步骤缺一不可：
- **2.1 异常值检测**：识别超出合理范围的数值（NaN 视为缺失，不计入异常）
- **2.2 缺失值填充**：用均值逼近法填补 NaN 标记的缺失字段
- 若跳过了 2.1，异常值可能被当作正常数据分析
- 若跳过了 2.2，缺失值（NaN）不会被填补，影响后续统计分析
